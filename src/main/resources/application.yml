micronaut:
  application:
    name: serasaRest

grpc:
  client:
    plaintext: true
    max-retry-attempts: 10
  channels:
    serasa:
      address: 'localhost:50051'
      plaintext: true
      max-retry-attempts: 10